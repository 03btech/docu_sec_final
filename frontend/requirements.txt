# UI Framework
PyQt6

# API Communication
requests

# ML (YOLOv8)
ultralytics
opencv-python
torch
torchvision
openvino  # Intel CPU optimized inference (2-4x faster than PyTorch CPU)
mediapipe  # Face mesh for gaze-away detection

# Charts
matplotlib

# Utilities
Pillow
mss

# Document Viewer Dependencies
PyMuPDF  # For PDF viewing (also known as fitz)
python-docx  # For DOCX viewing
qtawesome  # For icons (if not already installed)